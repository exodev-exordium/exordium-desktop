import { PerfectScrollbarDirective } from './perfect-scrollbar.directive';

describe('PerfectScrollbarDirective', () => {
  it('should create an instance', () => {
    const directive = new PerfectScrollbarDirective();
    expect(directive).toBeTruthy();
  });
});
